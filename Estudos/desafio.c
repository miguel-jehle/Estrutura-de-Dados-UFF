TAB * le_arv_in_str(char ){

}

//Recebe a cadeia de string sempre andando com o endereço, para ler o proximo caracter e ler recursivamente
//Se ler tal "<" cria o ponteiro raiz da chamada
//Se ler um valor, cria o no e coloca a info
//Se ler ">" retorna a chamada recursiva para ir para a raiz buscando criar os nos da direita


//if (<) r = le_arv_str(&char + 1);
//if(num){
    //cria no
    //preenche com a info
    //lembrar de ver como converter
//}

/*

if(<){
    Arv *r = NULL
    r = le_arv_in_str(&char + 1); Faço a chamada pro caracter da frente
    //talvez aqui "andar" com o caracter, pois no retorno do num, ele estagna aqui
}   
if(num){
    //Cria no
    //preenche 
    //
    retorna 
}
if(<) r->esq = le_arv_in_str(&char + 1);
if(<) r-> dir = le_arv_in_str(&char + 1);
if(>) return r




*/